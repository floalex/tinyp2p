// don't forget to uncommit this file when pushing to git
Kadnode1 finds the other kad node on the network that has the right ID, then asks that kad node 
for its bat node contact info. Then kad node one passes the contact info of that Bat node to its own Bat node.
